plugins {
    id 'aoi.java-conventions'
}

description = 'XML Data API'

dependencies {
    implementation("com.thoughtworks.xstream:xstream:1.4.21") {
        exclude group: "xmlpull"
        exclude group: "io.github.x-stream"
    }
}
